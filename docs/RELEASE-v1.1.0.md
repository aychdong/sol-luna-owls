发布已通过审阅的 **V1 + R4**：保留 V1 全部动作，修复眉羽、向下看的圆眼和左侧方向递进。直接使用批准文件，不再重画。

- **安装**：下载 `Sol-Luna-v1.1.0-install.zip`，解压后把 `sol`、`luna` 复制到 `~/.codex/pets/`；先备份旧文件夹。
- **预览**：[在线双语预览](https://aychdong.github.io/sol-luna-owls/) 或下载离线 HTML。
- **回退**：原始 V1 与 R3 回退包均在附件中。
- **素材**：完整历史 ZIP 保留图稿、提示词、审阅页和快照；SHA-256 清单用于校验下载。
- **验证**：正式文件与批准 R4 字节一致；每只 72 格动作与 V1 一致；相对 R3 仅五个方向变化。无损 WebP、时长、ZIP 与预览逻辑已检查；原生事件触发本轮未重新实测。

---

Publish the approved **V1 + R4**: original actions with brow-plume, round downward-eye and left-gaze progression repairs. Approved files are used directly, without redrawing.

- **Install:** extract the install ZIP and copy `sol` and `luna` into `~/.codex/pets/`. Back up existing folders first.
- **Preview:** [bilingual online preview](https://aychdong.github.io/sol-luna-owls/) or the offline HTML attachment.
- **Rollback:** original V1 and R3 packages are attached.
- **Sources:** the full history ZIP preserves artwork, prompts, review pages and snapshots. Verify downloads with the SHA-256 manifest.
- **Verified:** byte-identical approved R4 files; all 72 V1 action cells per owl retained; only five gaze cells differ from R3. Lossless atlases, timelines, ZIP payload and preview logic checked. Native triggers have not been retested.
